import 'package:flutter/material.dart';

class KeywordstPage extends StatelessWidget {
  const KeywordstPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child : Text("Keywords Page"),
      ),
    );
  }
}